---
'@autoguru/overdrive': minor
---

Internalise Track C (C-P8): repoint AutoSuggest (safe refs)/DateInput/DateTimeField onto semantic color.* tokens. Intent-derived refs, the runtime colour-override machinery, and the tenant-variant colours.background.light are retained on the legacy contract until the major.
