---
'@autoguru/overdrive': minor
---

The `Text` component has been rebuilt with polymorphic `as` prop, consistent
with Box. The `noWrap` prop has been deprecated as the `textWrap` style prop is
available in uplifted components.
