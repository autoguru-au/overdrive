compressionLevel: mixed

enableGlobalCache: true

nodeLinker: node-modules

# AutoGuru's own first-party packages are exempt from the npm minimum-age
# quarantine so freshly published internal versions can be installed.
npmPreapprovedPackages:
  - "@autoguru/*"

yarnPath: .yarn/releases/yarn-4.15.0.cjs
