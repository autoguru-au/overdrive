import { storiesOf } from '@storybook/react';
import React from 'react';

import { {{pascalCase name}} } from '.';

export default { title: 'Components|{{pascalCase name}}' };

export const standard = () => (
	<{{pascalCase name}}/>
);
