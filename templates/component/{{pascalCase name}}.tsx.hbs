import clsx from 'clsx';
import React, { memo, NamedExoticComponent } from 'react';

import styles from './{{pascalCase name}}.scss';

interface Props {
	example?: boolean;
}

export const {{pascalCase name}}: NamedExoticComponent<Props> = memo(() => {
	return (<p className={clsx(styles.root)}>
		New component {{name}}! 🎉 Edit lib/components/{{pascalCase name}}/{{pascalCase name}}.tsx
	</p>);
});
